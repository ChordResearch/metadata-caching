{
  "name": "@web3-metadata/commons",
  "version": "1.0.0",
  "description": "placeholder",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "run-s clean build:tsc",
    "build:tsc": "tsc -b",
    "clean": "rm -rf coverage dist",
    "lint": "eslint .",
    "lint:fix": "yarn lint --fix",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "dotenv": "^16.0.1",
    "jest": "^28.1.3",
    "web3": "^1.7.5"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^28.1.6"
  }
}
